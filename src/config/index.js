import env from '../env';

const {
  authEndPoint,
  messageReaderEndPoint,
  socketEndpoint,
  cdnEndPoint,
  chatManageEndpoint,
} = env.default;

export {
  authEndPoint,
  messageReaderEndPoint,
  socketEndpoint,
  cdnEndPoint,
  chatManageEndpoint,
};
